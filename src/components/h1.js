import React from 'react'
import styled from 'styled-components'

const SH1 = styled.h1`
  margin: 0;
  padding: 1.5rem;
  text-align: center;
`

export default ({ children }) => <SH1>{children}</SH1>
